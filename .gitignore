node_modules
node_modules/*
.DS_Store
build
dist

# Local env / secrets (never commit real values)
# Keep .env.example and worker/.dev.vars.example tracked as templates only
.env
.env.local
.env.*.local
worker/.dev.vars
worker/node_modules
worker/.wrangler

# Stitch / design export mocks (not runtime assets)
stitch-export/

# Cursor / VS Code local tooling (API keys live in mcp.json)
.cursor/
.vscode/mcp.json
.cursor/*.env
.cursor/.env*

# Internal build notes — keep on disk locally; do not publish to GitHub
CURSOR_PORTFOLIO_BUILD_PLAN.md
PORTFOLIO_FINISH_PLAN.md
CONTENT_PRESERVATION_CHECKLIST.md
