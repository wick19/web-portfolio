node_modules
node_modules/*
.DS_Store
build
dist

# Local env (Vite + Worker secrets)
.env
.env.local
.env.*.local
worker/.dev.vars
worker/node_modules
worker/.wrangler

# Stitch / design export mocks (not runtime assets)
stitch-export/

# Cursor / VS Code local tooling (do not publish)
.cursor/
.vscode/mcp.json
.cursor/*.env
.cursor/.env*

# Internal build notes (local only — not part of the public portfolio product)
CURSOR_PORTFOLIO_BUILD_PLAN.md
PORTFOLIO_FINISH_PLAN.md
CONTENT_PRESERVATION_CHECKLIST.md
